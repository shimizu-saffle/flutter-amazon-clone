class AuthService {
  // sign up user
  Future<void> signUpUser({
    required String email,
    required String password,
    required String name,
  }) async {
    try {
      // TODO(shimizu-saffle): implement signUpUser
    } catch (e) {
      print(e.toString());
      rethrow;
    }
  }
}
